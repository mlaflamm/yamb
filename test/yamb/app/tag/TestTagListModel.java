package yamb.app.tag;

import junit.framework.TestCase;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.Collections;
import java.util.List;

/**
 * @author manuel.laflamme
 * @since 24-Feb-2008
 */
public class TestTagListModel extends TestCase
{
/*    public void testGetInsertionPoint() throws Exception
    {
        List<String> list = new ArrayList<String>(Arrays.asList("A", "z", "d", "t", "1", "9"));

        System.out.println("Before: " + list);
        Collections.sort(list, String.CASE_INSENSITIVE_ORDER);
        System.out.println("Sorted: " + list);

        String value = "0";
        int index = Collections.binarySearch(list, value, String.CASE_INSENSITIVE_ORDER);
        System.out.println("Search: " + value + ", " + index + ", " + -(index + 1));

        // Not found! Add it!
        if (index < 0)
        {
            list.add(-(index + 1), value);
        }
        System.out.println("Insert: " + list);

    }*/
}
